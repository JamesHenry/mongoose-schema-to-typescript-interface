const expect = require('chai').expect
const generateTSInterface = require('./index')

describe( 'mongoose-schema-to-typescript-interface', () => {

    it( 'should work', () => {

        expect( true ).to.be.true

    })

})
